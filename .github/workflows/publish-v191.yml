name: Publish VitaFlightRadar v1.9.1

on:
  workflow_dispatch:
  push:
    branches: ["v191-build"]
    paths:
      - ".github/workflows/publish-v191.yml"

permissions:
  actions: read
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Check out main
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Download verified v1.9.1 build artifact
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          curl -L --fail \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/1000660928-design/VitaFlightRadar/actions/artifacts/10504915585/zip" \
            -o v191-artifact.zip
          unzip -q v191-artifact.zip -d v191-artifact
          test -f v191-artifact/VitaFlightRadar.vpk

      - name: Verify public VPK
        run: |
          python3 - <<'PY'
          import zipfile, hashlib
          p='v191-artifact/VitaFlightRadar.vpk'
          with zipfile.ZipFile(p) as z:
              assert z.testzip() is None
              assert 'default_location.txt' not in z.namelist()
              assert hashlib.sha256(z.read('sce_sys/icon0.png')).hexdigest() == '7918a7a0f1323bfa5f00b529578a5d0594ab49c54f357c3170508e650a50023d'
          print('Verified public v1.9.1 VPK')
          PY

      - name: Publish permanent download to main
        run: |
          mkdir -p releases
          cp v191-artifact/VitaFlightRadar.vpk releases/VitaFlightRadar-v1.9.1.vpk
          git config user.name "VitaFlightRadar Builder"
          git config user.email "actions@users.noreply.github.com"
          git add releases/VitaFlightRadar-v1.9.1.vpk
          git commit -m "Publish VitaFlightRadar v1.9.1 VPK" || exit 0
          git push origin main
